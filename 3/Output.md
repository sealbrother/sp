### Output.md

```markdown
## Output Explanation for c4.c

1. `nul 0`: 表示第一條指令（程式計數器的初始位置）執行後的狀態。
2. `nul 4223413`: 表示第二條指令執行後的狀態，以此類推。
3. `power(2, 5)=32`: 表示程式執行完畢後的結果，即 `power(2, 5)` 的結果為 32。
4. `exit(15) cycle = 0`: 表示程式結束，返回值為 15，執行指令的總數為 0。